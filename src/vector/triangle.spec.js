const {assert} = require('chai');
const {describe, it} = require('mocha');

describe('Triangle Tests', () => {
    describe('constructor', () => {});
    describe('[Method]: ', () => {});
    describe('[Method]: ', () => {});
});
